export interface ProfileModel {
    email: string
    timeCreate : number
    followings: number
    username: string
    id: string
    bio: string,
    placesVisited: number
    followers: number,
}

